--- # node weight tracker app definition
- hosts: webservers
  become: yes
  tasks:
    - name: get nodejs installation
      ansible.builtin.shell: curl -sL https://deb.nodesource.com/setup_14.x -o nodesource_setup.sh | sudo bash nodesource_setup.sh

    - name: install nodejs
      apt:
        name: nodejs
        state: present

pipeline {
  agent {label 'appslave'}
  stages{
    stage('Change permissions to Autoscript') {
      steps {
        sh 'pwd'
        sh 'echo chmod u+x ./AutoWeightTracker/WTAPPL.sh'
      }
    }
    stage('publish package') {
      steps {
        sh 'echo npm publish'
      }
    }
  }
}

pipeline {
  agent {label 'appslave'}
  stages{
    stage('initialization') {
      steps {
        sh 'echo npm init -y'
      }
    }
    stage('publish package') {
      steps {
        sh 'echo npm publish'
      }
    }
  }
}
